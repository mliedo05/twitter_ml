<nav class="navbar navbar-expand-lg navbar-light bg-danger">
    <div class="my-auto mx-4">
        <a class="navbar-brand text-white" href="<%=root_path %>"><i class="fab fa-twitter fa-2x"></i></a>
    </div>
    <div  class="ml-auto" >
        <% if user_signed_in? %>
            <%= link_to "log out", destroy_user_session_path, method: :delete, class: "btn btn-danger text-light"%>
            <%= link_to 'Crear Tweet', new_tweet_path, class:"btn btn-danger text-light ml-auto" %>
        <% else %>
            <%= link_to "Singn in", new_user_session_path, class: "btn btn-danger text-light"%>
            <%= link_to "Singn up", new_user_registration_path, class: "btn btn-danger text-light ml-auto"%>
            
        <% end %>
    </div>
</nav>